# ==========================================
# Panadería Rodrigo - Configuración General
# ==========================================
spring.application.name=PanaderiaRodrigo
server.port=8080

# ==========================================
# Base de datos H2 (desarrollo)
# ==========================================
spring.datasource.url=jdbc:h2:mem:panaderiadb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Consola H2 accesible en: http://localhost:8080/h2-console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

# ==========================================
# JPA / Hibernate
# ==========================================
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==========================================
# MySQL (descomentar para producción)
# ==========================================
# spring.datasource.url=jdbc:mysql://localhost:3306/panaderia_rodrigo?useSSL=false&serverTimezone=UTC
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=tu_password
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=update

# ==========================================
# Thymeleaf
# ==========================================
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.mode=HTML

# ==========================================
# Spring Security - usuarios básicos
# (solo si no se usa SecurityConfig con InMemory)
# ==========================================
# spring.security.user.name=rodrigo
# spring.security.user.password=admin123
